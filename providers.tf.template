terraform {
  required_providers {
    google = {
      source = "hashicorp/google"
      version = "4.47.0"
    }
  }
}

provider "google" {
  project     = "$PROJECT_ID"
  region      = "$LOCATION"  
  credentials = "$KEYRING_KEY"
}
